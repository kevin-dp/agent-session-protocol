{
  "name": "capi",
  "description": "Reference CLI implementation of the agent-session-protocol: share / import / resume agent coding sessions across machines and agents.",
  "version": "0.0.1",
  "license": "MIT",
  "type": "module",
  "bin": {
    "capi": "./bin/capi.mjs",
    "capi-queue-channel": "./bin/capi-queue-channel.mjs"
  },
  "files": [
    "dist",
    "bin",
    "skills"
  ],
  "dependencies": {
    "@durable-streams/client": "^0.2.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agent-session-protocol": "workspace:^",
    "zod": "^3.25"
  },
  "devDependencies": {
    "@durable-streams/server": "^0.3.0",
    "@types/node": "^22.15.21",
    "tsdown": "^0.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.5.2"
  },
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "test": "echo 'no unit tests; see test/*.ts for end-to-end scripts'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
